# Copy this file to catalogue/.env and fill in your own value (catalogue/.env
# is already excluded from git by the root .gitignore - never commit it).

# Get this from https://fal.ai/dashboard/keys after creating a fal.ai account.
FAL_KEY=replace-with-your-fal-api-key
